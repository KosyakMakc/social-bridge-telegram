name: publish Telegram social platform API

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Upload to Maven repository
        env:
          REGISTRY_TOKEN: ${{ secrets.MVN_PUBLISH_TOKEN }}
          VERSION: 0.1.0-SNAPSHOT-master-${{ github.run_number }}
        run: |
          ./gradlew :publish